# ESLint & Prettier 配置说明

## 📦 已安装的包

### ESLint 相关
- `eslint@^9` - 核心 linter
- `@eslint/js` - JavaScript 推荐规则
- `typescript-eslint` - TypeScript 支持
- `eslint-plugin-vue` - Vue 3 支持
- `eslint-plugin-prettier` - Prettier 集成
- `eslint-config-prettier` - 禁用与 Prettier 冲突的规则

### Prettier 相关
- `prettier@^3.6.2` - 代码格式化工具

## 🔧 配置文件说明

### `eslint.config.js` (ESLint 9 扁平配置)
使用最新的 ESLint 9 flat config 格式，包含：
- **JavaScript** 推荐规则
- **TypeScript** 推荐规则
- **Vue 3** 推荐规则
- **Prettier** 集成配置
- 自定义规则调整

### `.prettierrc`
代码格式化规则：
- 单引号
- 无分号
- 尾随逗号
- 80 字符行宽
- 2 空格缩进
- LF 换行符

### `.vscode/settings.json`
VSCode 编辑器配置：
- 保存时自动格式化
- 保存时自动修复 ESLint 错误
- 启用 ESLint flat config

## 🚀 使用命令

```bash
# 检查代码规范（不修复）
npm run lint:check

# 检查并自动修复代码规范
npm run lint

# 检查代码格式（不修复）
npm run format:check

# 格式化所有代码
npm run format
```

## 📝 规则说明

### TypeScript 规则
- ✅ `any` 类型：警告（允许使用但会提示）
- ✅ 未使用变量：警告（以 `_` 开头的除外）
- ✅ 函数返回类型：不强制要求
- ✅ 非空断言：警告

### Vue 规则
- ✅ 单词组件名：关闭（允许如 `Home.vue`）
- ✅ 必需默认 prop：关闭
- ✅ v-html：警告（安全提示）
- ✅ 自闭合标签：HTML 标签不自闭合，组件自闭合
- ✅ 每行最多 3 个属性（单行）

### 通用规则
- ⛔ `console.log`：警告（允许 `console.warn` 和 `console.error`）
- ⛔ `debugger`：警告
- ✅ 优先使用 `const`
- ⛔ 禁止使用 `var`

## 🎯 最佳实践

### 1. Git Hooks（可选）
可以添加 `husky` + `lint-staged` 实现提交前自动检查：

```bash
npm install -D husky lint-staged
npx husky init
```

在 `package.json` 添加：
```json
{
  "lint-staged": {
    "*.{js,ts,vue}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,css,scss}": [
      "prettier --write"
    ]
  }
}
```

### 2. CI/CD 集成
在 CI 流程中添加：
```bash
npm run lint:check
npm run format:check
npm run build
```

### 3. 编辑器配置
确保安装 VSCode 扩展：
- ESLint
- Prettier
- Volar (Vue)
- TypeScript Vue Plugin

## 🔍 故障排查

### ESLint 不工作？
1. 检查 VSCode 是否安装 ESLint 扩展
2. 重启 VSCode
3. 检查输出面板的 ESLint 日志

### Prettier 不格式化？
1. 检查文件是否在 `.prettierignore` 中
2. 确认 VSCode 默认格式化工具是 Prettier
3. 检查是否有语法错误阻止格式化

### 规则冲突？
ESLint 和 Prettier 已通过 `eslint-config-prettier` 完美集成，不应有冲突。

## 📚 参考资源

- [ESLint 官方文档](https://eslint.org/docs/latest/)
- [Prettier 官方文档](https://prettier.io/docs/en/)
- [Vue ESLint 插件](https://eslint.vuejs.org/)
- [TypeScript ESLint](https://typescript-eslint.io/)
